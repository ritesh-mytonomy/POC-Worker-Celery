[pytest]
testpaths = tests
pythonpath = .
# /srv is read-only to the app user in the image
# NFR-7 coverage gate (task 13.2). Partial runs, e.g. only tests/test_engine_*, add --no-cov.
addopts = -p no:cacheprovider --cov=engine --cov=workers --cov-report=term-missing --cov-fail-under=80
